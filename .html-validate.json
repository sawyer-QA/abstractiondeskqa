{
  "extends": ["html-validate:recommended"],
  "rules": {
    "void-style": ["error", { "style": "selfclosing" }]
  },
  "overrides": [
    {
      "include": ["lookup.html"],
      "rules": {
        "heading-level": "warn"
      }
    }
  ]
}
